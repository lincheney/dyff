commit 928964f4a1da4b3cbb81cce8a947fb1eacd900ec
Author: 林千里 <lincheney@gmail.com>
Date:   Sat Aug 30 01:25:43 2025 +1000

    make it kinda work on chrome

diff --git a/ffcli-server b/ffcli-server
index f882838..82d6824 100755
--- a/ffcli-server
+++ b/ffcli-server
@@ -117,6 +117,7 @@ async def main():
     loop = asyncio.get_event_loop()
 
     # find firefox profile dir
+    import json
     if crash_dir := os.environ.get('MOZ_CRASHREPORTER_EVENTS_DIRECTORY'):
         profile_dir = os.path.dirname(os.path.dirname(crash_dir))
     else:
@@ -127,6 +128,10 @@ async def main():
             if dest.endswith('/places.sqlite'):
                 profile_dir = os.path.dirname(dest)
                 break
+            if dest.endswith('/System Profile/SharedStorage'):
+                # chrome
+                profile_dir = os.path.dirname(os.path.dirname(dest))
+                break
 
     transport, protocol = await loop.connect_write_pipe(asyncio.streams.FlowControlMixin, sys.stdout.buffer)
     stdout = asyncio.streams.StreamWriter(transport, protocol, None, loop)
